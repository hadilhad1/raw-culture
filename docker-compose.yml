version: '3.9'

services:
  backend:
    image: node:20-alpine
    working_dir: /app
    volumes:
      - .:/app
    command: sh -lc "npm install && npm run dev --workspace backend"
    ports:
      - "4000:4000"
    environment:
      - PORT=4000
      - DATABASE_URL=file:./backend/prisma/dev.db
